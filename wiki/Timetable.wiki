#summary 2012190712 이성호 미소년 학생
#labels 2012190712,이성호

= Introduction =

시간표를 짜는 프로그램입니다.

1학점, 2학점, 3학점 세가지가 있습니다.

시간표가 겹치면 오류메세지가 뜹니다.

최대학점은 20학점입니다.

= Details =

Add your content here.  Format your content with:

* 시간표출력 클래스 : 목록을 실행하는 도중에도 목록 아랫부분에 계속 나옵니다. 요일은 행, 시간은 열로 나타냅니다. 2차원 배열로 나타내고 시작할땐 모두 null상태에서 시작합니다. 출력함수를 써서 목록출력클래스를 나타냅니다.
{{{
// 시간표를 출력하는 클래스입니다.
public class timeatable{
int timetable[][];
 for(i=0;i<){
  for(j=0;j){
   System.out.print(timetable[i][j]/t);
   }
   System.out.println();
   }
}
}}}

* 강의목록 입력 클래스 : 100개의 강의목록을 입력합니다. 혹은 파일에서 리스트를 가져옵니다.
{{{
/// 강의목록을 입력하는 클래스입니다.
public class intlist{
String list[]; // 강의 목록을 입력할 리스트입니다.
public write(String list){ // 입력할 리스트를 넘겨줍니다.
}}}

* 목록출력 클래스 : 시간표 위에 목록으로 나타냅니다. 1을 누르면 강의등록, 2를 누르면 강의삭제, 3을 누르면 수강가능한 리스트를 보여줍니다.
{{{
// 목록을 출력하는 클래스입니다.
public class printlist{
// 1~3까지의 번호를 입력받아 다른 메소드를 실행시키는 메소드입니다.
numinsert(){
 lecturein(){} // 1을 누르면 강의 등록으로 넘어갑니다.
 lecturedel(){} // 2를 누르면 강의가 삭제됩니다.
 lecturepossible(){}// 3을 누르면 수강가능한 리스트를 보여줍니다.
}

}}}

* 강의 등록 클래스 : 목록에서 1을 누르면 실행됩니다. 00부터 99까지의 번호중 하나를 선택해야 하며 이미 시간표에 등록되어 있는 시간이 겹치면 error메세지가 뜹니다. 또한 총 학점이 20학점이 넘어가면 error메세지가 뜹니다. x를 누르면 목록으로 돌아갑니다. 1차원 리스트로 만듭니다. 하나하나 정보를 구조체로.
{{{
public class lecturein{
  System.out.println("등록할 강의번호를 누르세요");
  
}}}
  

* 강의 삭제 클래스 : 목록에서 2를 누르면 실행됩니다. 등록한 강의중 선택한 번호의 강의를 삭제합니다. 등록되어있지 않은 강의번호를 선택하면 error메세지가 뜹니다. x를 누르면 목록으로 돌아갑니다.
{{{
public class lecturdel{
  int i;
  System.out.println("등록된 강의목록입니다");
  for(i=0;i<100;i++){
    System.out.println()
}
}}}

* 수강 가능한 강의 리스트를 보여주는 클래스 : 등록되어있지 않은 강의 리스트를 보여줍니다. 시간이 겹치는 강의는 보여주지 않고 20학점중 18학점이 차 있다면 3학점짜리 강의는 보여주지 않습니다.
{{{
public class lecturepossible{
  System.out.println("수강가능한 강의목록입니다");
}
}}}