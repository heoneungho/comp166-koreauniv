#labels 2009190708이하정
 그림 맞추기 - 2011년 2학기 자바 프로젝트 소개 페이지 


= Introduction =

명화 검색 프로그램에서 명화를 로드해서 조각을 나눠 맞추는 간단한 게임으로 계획을 수정했습니다.

= Details =
{{{
import java.awt.*;

import java.util.*;

import javax.swing.*;
import java.io.*;


}}}
 - 필요할 것으로 예상되는 패키지 - Head First Java GUI 부분 12, 13, 14장 공부중입니다.

필요한 변수들

{{{
      ArrayList<String> files ; // 사용자가 파일 제목(String)을 저장하는 것을 저장할 ArrayList

      int level; //사용자가 그림을 level*level으로 조각낼 수 있게 입력을 받는다. 조각이 많을 수록 어려우므로 level의 의미를 부여함.
      int numOfhits;//사용자가 몇번이나 조각을 옮겼는지 확인
      String grade;//사용자의 numOfhits에 따라 grade를 부여
      String result;//현재 결과 - ,다 맞추었는지 아직 완성이 덜되었는지를 저장해두는 문자열.
       Gamehelper gh = new GameHelper();//game helper 호출


}}}

gamehelper class -  조각을 섞고, 사용자의 입력을 받아서, 그림이 완성되면 finishgame()을 호출.
{{{

public class GameHelper{
  public void shuffle(){
 }//조각 섞는 메쏘드 - api에 있음.

public String checkYourself(){
    return result;

if(result == "Complete!"){
  finishGame();
 }//조각이 다 맞춰졌는지 확인



}//end of GameHelper
}}}


public class PuzzleGame{다
{{{
  public void menu(){

//메뉴는 1. 게임시작, 2. 그림추가 3. 그림삭제 4.종료 4가지를 보여주고 입력을 받아서 원하는 행동을 한다.

//메뉴의 번호를 입력받는다.
  int choice = Integer.parseInt(String [] args);

if( choice ==1){
  startGame(); // 
}else if(choice ==2 ){
  ArraryList의 이름.add();
}else if(choice == 3){
  ArraryList의 이름.remove();
}else if(choice ==4){
  return ;
}

public void startGame(){

  String level =  helper.getUserInput("레벨 입력");
 }//게임을 시작한다.

}
}//end of puzzle game 
}}}