#summary UpDownGame
#labels 2012190726김경민

= Introduction =

 많이 알려져있고 심심풀이로 할 수 있는 업다운 게임입니다.<br>
 너무 평범함을 피하기 위해 찬스라는 개념을 도입해 신선함을 주고자 만드는 게임입니다.



= Details =

 * 스테이지를 5개로 나눕니다.
 * 스테이지 1에서는 1~10, 2에서는 1~50, 3에서는 1~100, 4에서는 1~500, 5에서는 1~1000범위의 숫자 하나를 프로그램이 랜덤으로 정합니다.
 * 플레이어가 한 숫자를 입력하면 입력한 숫자보다 처음 정한 숫자가 크면 Up, 작으면 Down을 출력합니다.
 * 플레이어가 숫자를 맞출 때까지 혹은 맞출 수 있는 기회를 다 사용할 때까지 계속됩니다.
 * 맞춘 경우, 플레이어가 몇 번째에 맞췄는지를 출력합니다.
 * 맞추지 못하고 기회를 모두 소진한 경우, 게임오버가 뜨며 처음 정해진 숫자가 무엇인지 출력합니다.
 * 평범한 업다운 게임과 다르게 찬스 기회를 한 번 주고, 구현돼 있는 찬스들(6~7가지) 중 랜덤으로 하나의 찬스의 힌트를 플레이어에게 제공합니다.



= Chances =

 찬스의 종류들<br>

 * 정해진 숫자가 짝수인지 알려줍니다.
 * 정해진 숫자가 3의 배수인지 알려줍니다.
 * 정해진 숫자가 현재 정해진 숫자의 범위의 최댓값(Max)와 최솟값(Min)의 중간값(Mid)에 비해 큰지 작은지를 알려줍니다.
 (Ex) 현재 알고 있는 범위가 50~100 이라면 정해진 숫자가 75 이하인지 이상인지 알려줍니다. 만약 정해진 숫자가 75라면 같다고 출력하며 게임을 종료합니다.
 * 숫자를 맞출 수 있는 기회를 1회 추가합니다.
 * 숫자를 맞출 수 있는 기회를 1회 감소시킵니다.
 * 꽝


 
= Code =

{{{

package upanddown;

import java.util.Scanner;

public class MainUpDown {
        
        public static void main(String[] args) {
                
                
                UpDownGame UDG = new UpDownGame();
                Chances ch = new Chances();
                GameHelper helper = new GameHelper();
                
                int stage = 1; //라운드는 1부터
                boolean alive = true;
                
                System.out.println("업다운 게임에 오신걸 환영합니다!");
                System.out.print("게임을 하시려면 1을, 도움말을 보시려면 2를 입력하세요 : ");
                
                int nu = 0;
                Scanner sc = new Scanner(System.in);
                nu = sc.nextInt();
                
                while(nu != 1 && nu != 2) {
                        System.out.println("잘못 입력 하셨습니다.");
                        System.out.print("게임을 하시려면 1을, 도움말을 보시려면 2를 입력하세요 : ");
                        nu = sc.nextInt();
                }
          
                if(nu == 2) {
                  
                  System.out.println("");
                  System.out.println("          =도움말=");
                  System.out.println("-이 게임은 임의로 정해진 숫자를 맞추는 게임입니다.");//도움말 내용
                  System.out.println("-모두 5개의 스테이지로 이루어져 있으며, 각 스테이지마다 맞춰야 하는 숫자의 범위가 다릅니다.");
                  System.out.println("-찬스의 기회가 한 번 주어지며, 찬스는 0을 입력했을 때 6가지 찬스 중 임의로 한가지가 실행됩니다.");
                  System.out.println("-다섯 스테이지를 모두 클리어 할 경우 승리하며, 기회를 모두 사용하고 숫자를 맞추지 못하면 패배합니다.");
                  System.out.println("");
                  System.out.print("--준비가 되셨으면 1을 입력하여 게임을 시작합니다: ");

                  while(nu != 1) {
                	  nu = sc.nextInt();
                  }
                }
                
                if(nu == 1) {
                	
                	System.out.println("");
                	System.out.println("");
                	System.out.println("");
                	System.out.println("게임을 시작합니다.");
                	System.out.println("");
                	System.out.println("");
                	
                	boolean chv = true;
                  
                	while (alive == true) {
                		
                		System.out.println("== Stage " + stage + " ==");
                		
                		
                		
                		
                		switch(stage) {
	                		
	                		case 1 :
	                			
	                			UDG.max = 10;
	                			UDG.min = 1;
	                			UDG.life = 5;
	                			UDG.ans = (int) (Math.random() * 10) + 1;
	                			
	                			while(UDG.life > 0 && UDG.clear == false) {
	                				
	                				System.out.println("-life: " + UDG.life);
	                				
	                				do {
	                					
	                					System.out.println("-" + UDG.min + "에서 " + UDG.max + " 사이의 숫자를 맞춰야 합니다.");
		                				System.out.println("");
		                				
		                				UDG.guess = helper.getInput();
			                			System.out.println("");	
			                			
			                			if(UDG.guess == 0) {
			                				break;
			                			}
			                			
	                				} while(UDG.guess > UDG.max || UDG.guess < UDG.min);
	                				
	                				if(UDG.guess == UDG.ans) {
	                					UDG.clear = true;
	                					UDG.life--;
		                			} else if(UDG.guess == 0 && chv == true) {
		                				//찬스 사용!!
		                				ch.pch(UDG);
		                				chv = false;
		                				System.out.println("");
		                				UDG.numofguesses--;
		                			} else if(UDG.guess == 0) {
		                				System.out.println("!! 찬스 기회를 모두 사용하여 사용 하실 수 없습니다. !!");
		                				UDG.numofguesses--;
		                			} else if(UDG.guess < UDG.ans ) {
		                				System.out.println("== Up!! ==");
		                				UDG.min = UDG.guess;
		                				UDG.life--;
		                			} else{
		                				System.out.println("== Down!! ==");
		                				UDG.max = UDG.guess;
		                				UDG.life--;
		                			}
	                				
	                				UDG.guess = 0;
	                				UDG.numofguesses++;
     				
	                			}
	                			
	                			break;
	                			
	                			
	                		case 2 :
	                			
	                			UDG.max = 50;
	                			UDG.min = 1;
	                			UDG.life = 6;
	                			UDG.ans = (int) (Math.random() * 50) + 1;
	                			
	                			while(UDG.life > 0 && UDG.clear == false) {
	                				
	                				System.out.println("-life: " + UDG.life);
	                				
	                				while((UDG.guess > UDG.max || UDG.guess < UDG.min)) {
	                					
	                					System.out.println("-" + UDG.min + "에서 " + UDG.max + " 사이의 숫자를 맞춰야 합니다.");
		                				
	                					UDG.guess = helper.getInput();
			                			System.out.println("");	
			                			
			                			if(UDG.guess == 0) {
			                				break;
			                			}
			                			
	                				}
	                				
	                				if(UDG.guess == UDG.ans) {
	                					UDG.clear = true;
	                					UDG.life--;
		                			} else if(UDG.guess == 0 && chv == true) {
		                				//찬스 사용!!
		                				ch.pch(UDG);
		                				chv = false;
		                				System.out.println("");
		                				UDG.numofguesses--;
		                			} else if(UDG.guess == 0) {
		                				System.out.println("!! 찬스 기회를 모두 사용하여 사용 하실 수 없습니다. !!");
		                				UDG.numofguesses--;
		                			} else if(UDG.guess < UDG.ans ) {
		                				System.out.println("== Up!! ==");
		                				UDG.min = UDG.guess;
		                				UDG.life--;
		                			} else{
		                				System.out.println("== Down!! ==");
		                				UDG.max = UDG.guess;
		                				UDG.life--;
		                			}
	                				
	                				UDG.guess = 0;
	                				UDG.numofguesses++;
     				
	                			}
	                			
	                			break;
	                			
	                			
	                		case 3 :
	                			
	                			UDG.max = 100;
	                			UDG.min = 1;
	                			UDG.life = 7;
	                			UDG.ans = (int) (Math.random() * 100) + 1;
	                			
	                			while(UDG.life > 0 && UDG.clear == false) {
	                				
	                				System.out.println("-life: " + UDG.life);
	                				
	                				while((UDG.guess > UDG.max || UDG.guess < UDG.min)) {
	                					
	                					System.out.println("-" + UDG.min + "에서 " + UDG.max + " 사이의 숫자를 맞춰야 합니다.");
		                				
	                					UDG.guess = helper.getInput();
			                			System.out.println("");	
			                			
			                			if(UDG.guess == 0) {
			                				break;
			                			}
			                			
	                				}
	                				
	                				if(UDG.guess == UDG.ans) {
	                					UDG.clear = true;
	                					UDG.life--;
		                			} else if(UDG.guess == 0 && chv == true) {
		                				//찬스 사용!!
		                				ch.pch(UDG);
		                				chv = false;
		                				System.out.println("");
		                				UDG.numofguesses--;
		                			} else if(UDG.guess == 0) {
		                				System.out.println("!! 찬스 기회를 모두 사용하여 사용 하실 수 없습니다. !!");
		                				UDG.numofguesses--;
		                			} else if(UDG.guess < UDG.ans ) {
		                				System.out.println("== Up!! ==");
		                				UDG.min = UDG.guess;
		                				UDG.life--;
		                			} else{
		                				System.out.println("== Down!! ==");
		                				UDG.max = UDG.guess;
		                				UDG.life--;
		                			}
	                				
	                				UDG.guess = 0;
	                				UDG.numofguesses++;
     				
	                			}
	                			
	                			break;
	                			
	                			
	                		case 4 :
	                			
	                			UDG.max = 500;
	                			UDG.min = 1;
	                			UDG.life = 8;
	                			UDG.ans = (int) (Math.random() * 500) + 1;
	                			
	                			while(UDG.life > 0 && UDG.clear == false) {
	                				
	                				System.out.println("-life: " + UDG.life);
	                				
	                				while((UDG.guess > UDG.max || UDG.guess < UDG.min)) {
	                					
	                					System.out.println("-" + UDG.min + "에서 " + UDG.max + " 사이의 숫자를 맞춰야 합니다.");
		                				
	                					UDG.guess = helper.getInput();
			                			System.out.println("");	
			                			
			                			if(UDG.guess == 0) {
			                				break;
			                			}
			                			
	                				}
	                				
	                				if(UDG.guess == UDG.ans) {
	                					UDG.clear = true;
	                					UDG.life--;
		                			} else if(UDG.guess == 0 && chv == true) {
		                				//찬스 사용!!
		                				ch.pch(UDG);
		                				chv = false;
		                				System.out.println("");
		                				UDG.numofguesses--;
		                			} else if(UDG.guess == 0) {
		                				System.out.println("!! 찬스 기회를 모두 사용하여 사용 하실 수 없습니다. !!");
		                				UDG.numofguesses--;
		                			} else if(UDG.guess < UDG.ans ) {
		                				System.out.println("== Up!! ==");
		                				UDG.min = UDG.guess;
		                				UDG.life--;
		                			} else{
		                				System.out.println("== Down!! ==");
		                				UDG.max = UDG.guess;
		                				UDG.life--;
		                			}
	                				
	                				UDG.guess = 0;
	                				UDG.numofguesses++;
     				
	                			}
	                			
	                			break;
	                			
	                			
	                		case 5 :
	                			
	                			UDG.max = 1000;
	                			UDG.min = 1;
	                			UDG.life = 8;
	                			UDG.ans = (int) (Math.random() * 1000) + 1;
	                			
	                			while(UDG.life > 0 && UDG.clear == false && UDG.win == false) {
	                				
	                				System.out.println("-life: " + UDG.life);
	                				
	                				while((UDG.guess > UDG.max || UDG.guess < UDG.min)) {
	                					
	                					System.out.println("-" + UDG.min + "에서 " + UDG.max + " 사이의 숫자를 맞춰야 합니다.");
		                				
	                					UDG.guess = helper.getInput();
			                			System.out.println("");	
			                			
			                			if(UDG.guess == 0) {
			                				break;
			                			}
			                			
	                				}
	                				
	                				if(UDG.guess == UDG.ans) {
	                					UDG.win = true;
	                					UDG.life--;
		                			} else if(UDG.guess == 0 && chv == true) {
		                				//찬스 사용!!
		                				ch.pch(UDG);
		                				chv = false;
		                				System.out.println("");
		                				UDG.numofguesses--;
		                			} else if(UDG.guess == 0) {
		                				System.out.println("!! 찬스 기회를 모두 사용하여 사용 하실 수 없습니다. !!");
		                				UDG.numofguesses--;
		                			} else if(UDG.guess < UDG.ans ) {
		                				System.out.println("== Up!! ==");
		                				UDG.min = UDG.guess;
		                				UDG.life--;
		                			} else{
		                				System.out.println("== Down!! ==");
		                				UDG.max = UDG.guess;
		                				UDG.life--;
		                			}
	                				
	                				UDG.guess = 0;
	                				UDG.numofguesses++;
     				
	                			}
	                			
	                			break;
	
	                			
                		}
	                			
                             		
                		if(UDG.clear == true) {
                			System.out.println("== Stage " + stage + " 클리어!! ==");
                			System.out.println("-시도 횟수 : " + UDG.numofguesses + " -남은 기회 : " + UDG.life);
                			System.out.println("");
                			stage++;
                			UDG.numofguesses = 0;
                			UDG.clear = false;
                		} else if (UDG.win == true) {
                			System.out.println("== Stage " + stage + " 클리어!! ==");
                			System.out.println("-시도 횟수 : " + UDG.numofguesses + " -남은 기회 : " + UDG.life);
                			System.out.println("");
                			
                			System.out.println(" !! 모든 스테이지를 클리어 하셨습니다. !!");
                			System.out.println("!! 당신은 UpDown게임을 정벅하셨습니다. !!");
                			System.out.println("     !! You Win !!");
                			alive = false;
                			//승리로 끝남!
                		} else if(UDG.life == 0) { //패배시
                			alive = false;
                			System.out.println("- 정답은 " + UDG.ans + "입니다.");
                			System.out.println("- 패배하였습니다. -");
                			System.out.println("- Game Over -");
                			System.out.println("");
                		} 
                                                
                	} //while문 끝
                  
                } //if문 끝
                
        } //메인 메소드 끝

} //클래스 끝


package upanddown;

import java.util.Scanner;

public class GameHelper {
        
    public int getInput() {
    	
        int Input = 0;
        Scanner sc = new Scanner(System.in);
        System.out.println("  =숫자를 입력하세요.=  ");
        System.out.println("-찬스를 사용하시려면 0을 입력하세요.-");
        Input = sc.nextInt();
        
        return Input;
        
    } //함수 끝
        
} //클래스 끝


package upanddown;

public class UpDownGame {
	
	public int guess = 0;
	public int ans = 0;
	public int life = 0;
	public int max = 0;
	public int min = 0;
	public int numofguesses = 0;
	public boolean clear = false;
	public boolean win = false;
	

}




package upanddown;

public class Chances {
        
        int ranch = (int) (Math.random() * 6) + 1;
 
        public void pch(UpDownGame UDG) {
                
            switch(ranch) {
            
            case 1:
                    System.out.println("  --2의 배수 힌트--  ");
                    
                    if((UDG.ans % 2) == 0) {
                    	System.out.println("==답은 2의 배수 입니다.==");
                    } else {
                    	System.out.println("==답은 2의 배수가 아닙니다.==");
                    }
                    break;

            
            case 2:
                    System.out.println("  --3의 배수 힌트--  ");
                    
                    if((UDG.ans % 3) == 0) {
                    	System.out.println("==답은 3의 배수 입니다.==");
                    } else {
                    	System.out.println("==답은 3의 배수가 아닙니다.==");
                    }
                    
                    break;
            
            
            case 3:
                    System.out.println("  --기회 추가--  ");
                    System.out.println("==기회가 1회 추가 되었습니다!==");
                    
                    UDG.life++;
                    
                    break;
                    
            case 4:
                    System.out.println("  --기회 감소--  ");
                    System.out.println("==기회가 1회 감소 되었습니다 호갱님^^==");
                    
                    UDG.life--;
                    
                    break;
                    
            case 5:
            		int mid = (int) (UDG.max + UDG.min) / 2;
            	
            	
                    System.out.println("  --반띵 찬스!!--  ");
                    
                    if (UDG.ans > mid) {
                    	UDG.min = mid;
                    	System.out.println("==답은 " + UDG.min + " 보다 크고, " + UDG.max + " 보다 작습니다.==");
                    } else if(UDG.ans < mid) {
                    	UDG.max = mid;
                    	System.out.println("==답은 " + UDG.min + " 보다 크고, " + UDG.max + " 보다 작습니다.==");
                    } else {
                    	UDG.clear = true;
                    } 
                    
                    break;
                    
            case 6:
                    System.out.println("  아쉽지만 꽝이네요.  ");
                    break;

                    

            } //스위치문 끝
                
        } //함수 
        
} //클래스 끝



}}}


일단 완성. 수정할 부분 있으면 차후 수정하겠습니다.